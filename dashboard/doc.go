// Package dashboard provides the administration dashboard as assets embedded in
// the Momobase binary.
//
// The assets are a Vite build of the React application in this directory. dist is not
// committed, so `make dashboard` and the build workflows generate it before compiling
// Go; without it this package does not build.
package dashboard
